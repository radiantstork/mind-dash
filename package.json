{
    "scripts": {
        "install": "npm install --prefix client && pipenv install --prefix server",
        "start_server": "cd server && python manage.py runserver",
        "start_client": "cd client && npm run dev",
        "dev": "npm run start_client & npm run start_server",
        "test": "npm run test --prefix client && pipenv run pytest -v --prefix server"
    }
}
